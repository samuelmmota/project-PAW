FROM mariadb:10.2

ENV MYSQL_ROOT_PASSWORD ${DB_PASSWORD}
ENV MYSQL_DATABASE ${DB_DATABASE}
ENV MYSQL_USER ${DB_USERNAME}
ENV MYSQL_PASSWORD ${DB_PASSWORD}

COPY ./my.cnf /etc/mysql/my.cnf

CMD ["mysqld", "--port=8080"]
